# Limecraft Codex Instructions

- Always do as much work as you can, with little breaks in between.
- Always say what is missing, and if the user specifies so then simply keep generating code until the selected tasks are complete.
- Whenever uploading or publishing a new release, increment the version by `0.1` using Minecraft-style numbering:
  - `1.1`, `1.2`, `1.3`, ..., `1.9`, `1.10`, `1.11`, ...
  - Do not treat `1.10` as `2.0`.
  - If the current uploaded release is `1.1`, the next uploaded release must be `1.2`.
